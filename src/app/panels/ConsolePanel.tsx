import { Panel } from "app/stores";
import { View } from "app/views";
import React from "react";

export const ConsolePanel = Panel.create("Console", () => 
{
	return (
		<View>
			Console
		</View>
	);
});